<% @links.each do |link| %>
	<div class="image_link">
		<%= link_to image_tag(link.url,alt: "a picture url", id: "tagline"), link, :remote => true, method: :delete, data: { confirm: "Are you sure?" } %>
	</div>
<% end %>
