<% @links.each do |link| %>
		<%= link_to image_tag(link.url,alt: "a picture url", id: "tagline",  :class=>"image_link"), link, :remote => true, method: :delete, data: { confirm: "Remove?" } %>
<% end %>
